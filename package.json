{
  "name": "mantis-api-test",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "mocha --timeout 40000",
    "test-html": "mocha --timeout 40000 --reporter mochawesome",
    "test-junit": "mocha --timeout 40000 --reporter mocha-junit-reporter",
    "lint": "standard",
    "lint:fix": "standard --fix"
  },
  "repository": {
    "type": "git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chai": "^4.3.4",
    "joi": "^17.5.0",
    "lint": "^0.7.0",
    "mocha": "^8.4.0",
    "mocha-junit-reporter": "^2.0.0",
    "mochawesome": "^6.1.1",
    "supertest": "^6.1.3"
  }
}
